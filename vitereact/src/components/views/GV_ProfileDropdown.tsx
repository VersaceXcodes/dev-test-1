<button type="button" onClick={toggleDropdown}>
  {/* Avatar Image */}
  <span className="sr-only">Account menu</span>
</button>

{isOpen && (
  <div className="absolute right-0 mt-2 w-56 origin-top-right rounded-md bg-white shadow-lg transition-all">
    {/* User Info */}
    {/* Menu Items */}
  </div>
)}